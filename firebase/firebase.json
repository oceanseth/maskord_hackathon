{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "database": {
    "rules": "database.rules.json"
  },
  "_comment": "IMPORTANT: Do NOT run 'firebase deploy --only firestore' or 'firebase deploy --only database' or 'firebase deploy --only storage' from this repo. Rules are owned and deployed by oceanseth/masky_auth. Only deploy functions from this repo.",
  "functions": [
    {
      "source": "functions",
      "codebase": "maskord",
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log"
      ]
    }
  ],
  "emulators": {
    "auth": { "port": 9099 },
    "functions": { "port": 5001 },
    "firestore": { "port": 8080 },
    "database": { "port": 9000 },
    "storage": { "port": 9199 },
    "ui": { "enabled": true, "port": 4000 }
  }
}
